import React, { View, Text } from 'react';

const Categories = () => {
    return(
        <View>
            <Text style={{flex: 1, justifyContent: 'center'}}>CATEGORIES</Text>
        </View>
    );
};

export default Categories;